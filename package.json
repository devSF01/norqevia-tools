{
  "name": "norqevia-work-data-tools",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/list-compare.test.mjs tests/serve.test.mjs",
    "test:network": "node tests/network-isolation.mjs",
    "test:browser": "playwright test",
    "test:all": "pnpm run build && pnpm run test && pnpm run test:network && pnpm run test:browser",
    "serve": "node scripts/serve.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
